extends layouts/main
include mixin/videoBlock

block content
    .vidoes
        each item in videos
            +videoBlock({
                title: item.title,
                views: item.views,
                videoFile: item.videoFile
            })
extends layouts/main
include mixin/videoBlock

block content
    .vidoes
        each item in videos
            +videoBlock({
                id: item.id,
                title: item.title,
                views: item.views,
                videoFile: item.fileUrl
            })